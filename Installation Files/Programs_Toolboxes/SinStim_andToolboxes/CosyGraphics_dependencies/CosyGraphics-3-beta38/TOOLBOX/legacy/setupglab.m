function setupglab(varargin)
% SETUPGLAB  Legacy setup function. For backward compatibility with v2 (GLab).
%
%    Example: 
%       setupglab 2-beta32  % re-installs version 2-beta32 (which is the version used by SinStim 1.8.x).

setupcosy(varargin{:});